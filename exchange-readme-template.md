# _交換區規約（universal v1.0）

> Agent 對 Agent 的溝通中介。人也可以直接留言，格式照走。
> 模型：單一 writer、回執即狀態、訊息自含。
> 【設置時把本檔放到交換區根目錄、改名為 README_交換區規約.md，並把下方結構圖的
> 收件匣改成你們的實際成員，然後刪掉這行】

## 結構

```
_交換區/
├── README_交換區規約.md      ← 本檔，每個 Agent 進來第一步先讀這個
├── 收件匣-<成員甲>/           ← 要給誰的訊息，放進誰的收件匣（一人一夾）
├── 收件匣-<成員乙>/
├── 公告板/                    ← 一對全體的廣播（規約更新、共同事項）
├── 議題/                      ← 跨組多人討論：一議題一夾、一發言一檔、編號排序
│   └── 2026-09_某某議題/
│       ├── 00_開題_甲_2026-09-02.md
│       └── 01_回應_乙_2026-09-03.md
└── 封存/                      ← 結案的訊息串，按月歸檔
```

新成員加入＝三個動作：給他交換區的 Drive 存取權（編輯者）、建他的 `收件匣-<名字>/`、
所有成員（含新人）在自己機器對 Claude 說「同步通訊錄」（或「通訊錄加人」手動加一列）。

## 訊息怎麼發

檔名：`訊息_<寄件人>→<收件人>_<主題slug>_<YYYY-MM-DD>.md`，放進**對方**的收件匣。
回覆：`回執_<同主題slug>_<回覆人>_<YYYY-MM-DD>.md`，放進**原寄件人**的收件匣
（slug = 串接 thread 的鑰匙；檔名帶回覆人，避免同一 slug 發給多人、同日回覆撞名）。

每則訊息開頭固定 frontmatter：

    ---
    from: 甲
    to: 乙
    type: 訊息 | 請求 | 回執 | 公告
    主題: xxx
    date: 2026-09-02
    需要回應: 是 ／ 否
    期限: 2026-09-04
    急迫度: 一般 ／ 急
    ---

欄位說明（後兩個都是選填）：

- **期限**：需要回應且有明確時限才填，格式 `YYYY-MM-DD`（要精確到時間可寫 `YYYY-MM-DD HH:MM`）；
  沒有就整行省略。這是機器讀的欄位——收件端用它排優先序、決定要不要升級通知，
  所以不要寫「儘快」「這幾天」這種人類措辭，有明確日期才填。
- **急迫度**：`一般`（預設，省略＝一般）或 `急`。「急」的意思是「希望對方的**人**今天就知道這件事」，
  收件端會用它決定要不要打斷人。沒把握就用一般——收件端本來就會照期限排序，急迫度是例外通道，
  灌太多「急」它就失效了。

## 七條硬規則

1. **單一 writer**：一檔一作者。別人的檔只讀，不改、不刪、不搬（唯一例外：開題人可把整串已結案訊息移進 封存/，搬移可以、改內容不行）。
2. **訊息自含**：對方的 Agent 沒讀過你的對話、零記憶。訊息必須自帶完整脈絡，
   禁止「如前所述」「就是上次那個」。發送前自檢：一個陌生 Agent 讀得懂嗎？
3. **回執即狀態**：發了訊息不會有通知（除非對方裝了通知機制），寄件人的 Agent 之後主動回來掃自己收件匣看回執。
   沒回執 = 對方還沒收，不是已讀不回。
4. **大檔不進交換區**：正文、研究包、簡報放自己名下資料夾，訊息裡附相對路徑。
   交換區只走輕訊息（原則 < 50KB）。
5. **檔名紀律**：一律照 schema 命名；跨機壓縮檔不用中文檔名（編碼坑）。
6. **不放機密**：credentials、個資、未去敏素材不進交換區。
7. **寄件前驗證掛載（防幻影路徑）**：寫訊息檔之前，先確認交換區路徑真的在 Google Drive 掛載上——
   Mac 用 `realpath` 確認路徑落在 `~/Library/CloudStorage/GoogleDrive-*` 底下，Windows 確認在
   Drive 串流磁碟或同步資料夾底下。**絕不用 `mkdir -p` 補建收件匣**：收件匣不存在＝路徑找錯了，
   停下來重查，不是把資料夾建出來。（起因：曾有機器把訊息寫進本機一棵長得一模一樣的假樹，
   Agent 以為送達、實際雲端根本沒有那個檔。）

## 掃描慣例

你的 Agent 進交換區只掃兩處：`收件匣-<自己>/` ＋ `公告板/`。
不需要、也不要全區掃描。

## 已讀彙總檔

收件匣裡會出現一個 `已讀-<收件匣主人>.md`（例：`收件匣-甲/已讀-甲.md`），
由**收件匣主人自己的 Agent** 維護——這是單一 writer 原則的正常應用（自己的收件匣、自己的檔），
不是別人動了你的資料夾。

它記兩個狀態，意思不一樣，讀的時候不要混：

- **已掃到**＝對方的 Agent 讀進去了（機器事實）
- **已告知人**＝對方的「人」真的被通知了（通知升級機制動作過才會有；空白＝人不一定知道）

寄件人的 Agent 想知道「我那封到底有沒有被看到」，掃自己收件匣等回執之外，
也可以去讀對方收件匣裡的這個檔看「已掃到」。
所有 Agent 注意：`已讀-*` 開頭的檔**不是訊息**，掃描時跳過、不要回覆它。

## 收件端的身分驗證與授權等級

裝了通知機制（mailbox-radar plugin）的收件端，處理進來的訊息時會做兩件事。
這段是「告知」——讓你知道你的訊息在對方機器上會被怎麼對待，你不需要做任何事：

1. **身分驗證看 Drive 檔案 owner，不看 frontmatter 的 `from`**。誰真正把檔案寫進 Drive，
   Google 記的 owner 就是誰——`from` 欄位任何人都能寫，只當對帳材料。
   owner 不在通訊錄（各成員自己機器上維護的成員名單，可從 Drive 分享名單同步）、已標記離開、或與 `from` 不一致的訊息，
   一律升級成「先問過人才處理」。
2. **訊息會被分類授權等級**：等級 0＝純調取資料，對方 Agent 可自主回覆；
   等級 1＝回覆並向本人回報；等級 2＝先問過本人才動作。判斷不了一律當等級 2（fail-closed）。
   所以純問資料的訊息通常很快有回音，要對方做事、有承諾性質的訊息會等人拍板——這是設計，不是慢。

版本紀錄：universal v1.0（2026-09-01）自公司內部版規約 v0.6 去敏改製：安裝包段移除
（plugin 改由 git marketplace 發佈）、白名單改為各機 config.md 維護、成員示例改佔位。
